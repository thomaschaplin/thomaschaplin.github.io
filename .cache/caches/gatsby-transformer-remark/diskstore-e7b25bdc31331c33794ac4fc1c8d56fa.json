{
  "expireTime": 9007200854252764000,
  "key": "transformer-remark-markdown-html-5d5260e04631df87efa3db286ea2f338-gatsby-remark-imagesgatsby-remark-external-linksgatsby-remark-prismjs-",
  "val": "<h1>What is nvm? 🤔</h1>\n<p><em>nvm is a version manager for node.js, designed to be installed per-user, and invoked per-shell. nvm works on any POSIX-compliant shell (sh, dash, ksh, zsh, bash), in particular on these platforms: unix, macOS, and windows WSL.</em></p>\n<h1>Why should I use nvm? 🤔</h1>\n<p>If you're developing software using the NodeJS runtime environment you're probably aware that you might want have multiple projects using different node versions, say one using <code class=\"language-text\">10</code> and another <code class=\"language-text\">12</code>. This could be due to a number of reasons. It gets annoying switching between node versions manually.</p>\n<h1>Manual usage of nvm 🐌</h1>\n<p>The project nvm solves this problem by allowing the user to install node versions on the fly by using a simple command:</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">nvm install &lt;NODE_VERSION_NUMBER&gt;</code></pre></div>\n<p>You can also easily switch between node versions using a simple command:</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">nvm use &lt;NODE_VERSION_NUMBER&gt;</code></pre></div>\n<h1>Automated usage of nvm 🤖</h1>\n<p>Let's face it, you're going to forget to switch to the correct node version at some point.</p>\n<p>We can in a sense, automate the switching between versions.</p>\n<p>It's a simple solution which only requires a one-time setup:</p>\n<ol>\n<li>Create a <code class=\"language-text\">.nvmrc</code> file in the root of your project folder with the version number you want to use</li>\n</ol>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">12</code></pre></div>\n<ol>\n<li>Add the below snippet to your <code class=\"language-text\">.zshrc</code> file</li>\n</ol>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">if [[ -f ~/.nvm/nvm.sh ]]; then\n\nsource ~/.nvm/nvm.sh\n\nautoload -U add-zsh-hook\nload-nvmrc() {\n  local node_version=&quot;$(nvm version)&quot;\n  local nvmrc_path=&quot;$(nvm_find_nvmrc)&quot;\n\n  if [ -n &quot;$nvmrc_path&quot; ]; then\n    local nvmrc_node_version=$(nvm version &quot;$(cat &quot;${nvmrc_path}&quot;)&quot;)\n\n    if [ &quot;$nvmrc_node_version&quot; = &quot;N/A&quot; ]; then\n      nvm install\n    elif [ &quot;$nvmrc_node_version&quot; != &quot;$node_version&quot; ]; then\n      nvm use\n    fi\n  elif [ &quot;$node_version&quot; != &quot;$(nvm version default)&quot; ]; then\n    echo &quot;Reverting to nvm default version&quot;\n    nvm use default\n  fi\n}\nadd-zsh-hook chpwd load-nvmrc\nload-nvmrc\n\nfi</code></pre></div>\n<p>Now when you change directory it will look for a <code class=\"language-text\">.nvmrc</code> file automatically and switch to the correct version.</p>\n<div class=\"gatsby-highlight\" data-language=\"text\"><pre class=\"language-text\"><code class=\"language-text\">cd /Users/thomaschaplin/projects/dummy_project\nFound &#39;/Users/thomaschaplin/projects/dummy_project/.nvmrc&#39; with version &lt;12&gt;\nNow using node v12.18.2 (npm v6.14.5)</code></pre></div>"
}
